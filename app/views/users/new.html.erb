<!-- .header-link= link_to "User Index", "/users/index" -->
<!-- <h2>New User</h2>

<%= form_for @user, :url => "/users/save/user.id" do |f| %>
  <%= f.text_field :username %>
  <%= f.text_field :password %>
  <%= f.submit "Create"%>
<% end %> -->
<!-- , :url => { :action => "new" }, :html => {:class => "nifty_form"} -->
<h1>New User</h1>

<%= render 'form' %>

<%= link_to 'Back', users_path %>
