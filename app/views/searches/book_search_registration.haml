.big-content
	.search-title.left.active= "BOOKS SEARCH RESULTS"
	.right
		= render "shared/search_form_registration"
	.clearboth
	- i = 0
	- @books.each do |book|
		- i = i + 1
		.small-content.left
			.img.left= image_tag book.image_url, :size => "60x60"
			.text-content.left
				.h1.link= link_to "#{book.title.truncate(15).upcase}", "/books/show/#{book.id}"
				.h3= book.author.truncate(25) unless book.author.nil?
				.h2= book.description.truncate(50) unless book.description.nil?
				.h2.link= link_to "Regist it", "/photo_registrations/new/#{book.id}", :method => "post"
		- if(i % 3 == 0)
			.clearboth
	.clearboth
	.paginate.right= will_paginate @books