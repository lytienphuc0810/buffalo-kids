.big-content
	.header-link= link_to "Back to Book Reservations index", "/book_reservations/index/1"
	.content
		.h1= @book.title
		.h2= @book.book_code
		.h2= @book.description
		.h2= @book.author
		.h2= @book_reservation.start_date.to_s
		.h2= @book_reservation.due_date.to_s
		.h2.link= link_to "Delete it", "/book_reservations/delete/#{@book_reservation.id}", :method => "post"